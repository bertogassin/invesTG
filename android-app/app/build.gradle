plugins {
    id 'com.android.application'
}

android {
    namespace 'de.resursmap.app'
    compileSdk 35

    defaultConfig {
        applicationId "de.resursmap.app"
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
